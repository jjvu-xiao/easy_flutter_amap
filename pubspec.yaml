name: easy_flutter_amap
description: 简单实用,好用的flutter高德地图插件
version: 0.0.1
author: xiaobing<m18779334084@163.com>
homepage: https://github.com/jjva-xiao/easy_flutter_amap

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' and Android 'package' identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: cn.jjvu.xiao.easy_flutter_amap
        pluginClass: EasyFlutterAmapPlugin
      ios:
        pluginClass: EasyFlutterAmapPlugin